import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const WSalary = ({
    
}) => {
  return (
    <View>
      <Text>WSalary</Text>
    </View>
  )
}

export default WSalary

const styles = StyleSheet.create({})